I've added mimelib (based on mime++) and uulib to kdesupport, 
because krn and kmail are using them.

I've added --foreign and --include-deps to automake to satisfy
FreeBSD and SGI fans.

updated mimelib to 970710
